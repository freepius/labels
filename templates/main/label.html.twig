{% extends 'base.html.twig' %}

{# le bloc head parent n'existe pas => @todo #}
{# {% block head %}
    <link rel="stylesheet" href="{{ asset('css/#{label.0}.css') }}">
{% endblock %} #}

{% block body_class %}container-label{% endblock %}

{% block body %}
    <nav>
        <ul>
            {% for label in labels %}
                <li>
                    <a href="/label/{{ label.id }}">{{ label.id }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>

    {{ include(label.templateName, sandboxed = true) }}
{% endblock %}
